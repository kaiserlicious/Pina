

if(NOT DEFINED PINA_PREFIX)
  SET(PREFIX "FooBar")
endif()

if(NOT DEFINED PINA_SUBNAMESPACES)
 Set(PINA_SUBNAMESPACES true)
else()
  if(PINA_SUBNAMESPACES)
    Set(PINA_SUBNAMESPACES true)
  else()
    Set(PINA_SUBNAMESPACES false)
  endif()
endif()

if(NOT DEFINED FX)
 Set(FX true)
else()
  if(FX)
    Set(FX true)
  else()
    Set(FX false)
  endif()
endif()

if(NOT DEFINED PHYSICS)
 Set(PHYSICS true)
else()
  if(PHYSICS)
    Set(PHYSICS true)
  else()
    Set(PHYSICS false)
  endif()
endif()

if(NOT DEFINED BREP)
 Set(BREP true)
else()
  if(BREP)
    Set(BREP true)
  else()
    Set(BREP false)
  endif()
endif()

if(NOT DEFINED KINEMATICS)
 Set(KINEMATICS true)
else()
  if(KINEMATICS)
    Set(KINEMATICS true)
  else()
    Set(KINEMATICS false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_DEBUG_LOG)
  Set(PINA_ENABLE_DEBUG_LOG true)
else()
    Set(PINA_ENABLE_DEBUG_LOG true)
  if()
  else()
    Set(PINA_ENABLE_DEBUG_LOG false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_INFO_LOG)
  Set(PINA_ENABLE_INFO_LOG true)
else()
    Set(PINA_ENABLE_INFO_LOG true)
  if()
  else()
    Set(PINA_ENABLE_INFO_LOG false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_ERROR_LOG)
  Set(PINA_ENABLE_ERROR_LOG true)
else()
    Set(PINA_ENABLE_ERROR_LOG true)
  if()
  else()
    Set(PINA_ENABLE_ERROR_LOG false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_SEVERE_LOG)
  Set(PINA_ENABLE_SEVERE_LOG true)
else()
    Set(PINA_ENABLE_SEVERE_LOG true)
  if()
  else()
    Set(PINA_ENABLE_SEVERE_LOG false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_FATAL_LOG)
  Set(PINA_ENABLE_FATAL_LOG true)
else()
    Set(PINA_ENABLE_FATAL_LOG true)
  if()
  else()
    Set(PINA_ENABLE_FATAL_LOG false)
  endif()
endif()

