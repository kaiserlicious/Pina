

if(NOT DEFINED PINA_PREFIX)
  SET(PREFIX "FooBar")
endif()

if(NOT DEFINED PINA_ENABLE_DEBUG_LOG)
  Set(PINA_ENABLE_DEBUG_LOG true)
else()
    Set(PINA_ENABLE_DEBUG_LOG true)
  if()
  else()
    Set(PINA_ENABLE_DEBUG_LOG false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_INFO_LOG)
  Set(PINA_ENABLE_INFO_LOG true)
else()
    Set(PINA_ENABLE_INFO_LOG true)
  if()
  else()
    Set(PINA_ENABLE_INFO_LOG false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_ERROR_LOG)
  Set(PINA_ENABLE_ERROR_LOG true)
else()
    Set(PINA_ENABLE_ERROR_LOG true)
  if()
  else()
    Set(PINA_ENABLE_ERROR_LOG false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_SEVERE_LOG)
  Set(PINA_ENABLE_SEVERE_LOG true)
else()
    Set(PINA_ENABLE_SEVERE_LOG true)
  if()
  else()
    Set(PINA_ENABLE_SEVERE_LOG false)
  endif()
endif()

if(NOT DEFINED PINA_ENABLE_FATAL_LOG)
  Set(PINA_ENABLE_FATAL_LOG true)
else()
    Set(PINA_ENABLE_FATAL_LOG true)
  if()
  else()
    Set(PINA_ENABLE_FATAL_LOG false)
  endif()
endif()

